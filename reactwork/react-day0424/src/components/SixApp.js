import { Alert } from '@mui/material';
import React from 'react';

const SixApp = () => {
    return (
        <div>
            <Alert severity='success'
                        style={{fontSize:'25px'}}>SixApp</Alert>
        </div>
    );
};

export default SixApp;